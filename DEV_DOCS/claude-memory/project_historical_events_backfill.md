---
name: project_historical_events_backfill
description: "Backfill status of past events (DI#1 ticketing, Maxwell House, showcases) + new series options"
metadata: 
  node_type: memory
  type: project
  originSessionId: 23f44bb5-a672-44a4-9c2e-b8eac9975d80
---

Backfilling past-event data (2026-06-24). New series options added (commit 60661e2): **'Come With Production'** (events produced as a SERVICE for other people, e.g. Maxwell House 4/20) and **'Content Creation'** (recorded/content sets). **Come With Production semantics (per Keith):** Come With was NOT involved in marketing/ticket sales — it's bringing/setting up equipment, possibly hosting. So tickets/donations/sponsors/marketing belong to the HOST, not Come With. The event-hub Overview (commit e045bab) detects type=production/series='Come With Production' and shows only Service income / Expenses / Net to Come With / Equipment provided / Event attendance — hiding ticket revenue, tickets sold, sell-through, gross & total-raised. Don't load host ticket/donation/sponsor data into these events; only Come With's service income + expenses + equipment_usage. `seriesToType()` maps series→events.type on save; edit-event modal now auto-fills capacity from the chosen venue.

**Events page redesigned as the command center (commit a92843d):** custom `loadEventsDash`/`renderEventsDash` (replaced the generic table; dropped Bar min). Top aggregate KPI cards over the filtered set (Events, Attendance, Tickets, Gross revenue, Net P&L, Raised for MS, Marketing spend); filters = search/series/status/year. **Type-adaptive "Result" metric convention** (`eventResult()`): party→net P&L, Dance Infusion→net to MS, Come With Production→net to CW, Content Creation→IG reach (sum of per-event IG snapshots). Reuse this convention for any new per-event headline. **DI vs business kept fully separate** (commit 6afc295): aggregates split into Business (parties/production/content → Revenue + Net P&L) and Dance Infusion (charity → Total raised + To MS). Two distinct money models — never mix them.

**Expenses → events:** `expenses.event_id` links an expense to an event. Expenses tab rebuilt as a custom loader (`loadExpenses`) with an inline per-row **Event dropdown** (`setExpenseEvent`), an event filter incl. "Unassigned" (highlighted), search, and totals. **`expenses.event_na`** (migration 050) = "not applicable to events" (overhead/capital); Software/Equipment defaulted to true. Expenses tab dropdown = needs-event / N/A(overhead) / <event>; filter "⚠ Needs an event" = event_id null AND NOT event_na. After this: 13 assigned, 43 N/A overhead, **12 truly need an event** ($262, all Elements Festival prof-dev). 7 event-specific expenses auto-assigned by description match (Crossroads/Maxwell/DI Showcase/DI#1); the overhead = business capital (CDJs/B&H/Sweetwater, Beatport/Ableton software, prof-dev) — those are NOT event-specific. Source data: `Financial/Simplifi - Transactions.csv` (cols Date,Account,Payee,Category,Exclusion,Recurring,Amount,Notes; 7098 rows, mostly personal — the 68 expenses were curated to business only). No in-app Simplifi importer yet (would need category/account filter rules from Keith + dedupe). **Simplifi import 2026-06-24** (`Simplifi - Transactions__20260624_191909.csv`, all txns since 4/1): work rows = Simplifi Category=='Work Expenses' (63 of 580); deduped by (date, abs amount) vs existing — 0 dups; inserted 63 ($4,707) each with description suffix `[simplifi 0624]` (reversible: delete where description like '%[simplifi 0624]%'). Auto-classified: Software/Equipment/Supplies→event_na; Facebook→Marketing; Elements→Prof Dev; Uber→Travel. 5 auto-assigned to events from Notes (Sq*zion→DI Showcase, Sq*acoustik→Maxwell). 23 left needs-event (ambiguous: Ubers, Tst*signal dining, Tce Presents, $500 Facebook ad, Elements) for Keith to assign in the Expenses UI. FUTURE imports: filter Category=='Work Expenses', dedup on (date, abs amount) vs existing expenses.

**Income/Expenses/Artists rebuilt (2026-06-25, commit 77d79d8, migration 053):** all three converted to custom loaders. **Income** (`loadIncome`): inline edit (amount/category/description), inline Payer(actor)+Event dropdowns, filters, by-category aggregates; deleted the 4 legacy $0 "description" rows (only $1.89 bank-interest real income remains — income is the surface for unlogged bar/vendor/merch/**service fees**). **Expenses**: added inline edit (amount/category/vendor/description), **vendor→actor link** (`expenses.vendor_actor_id`), receipt view/upload (receipts bucket), spend-by-category cards — on top of the earlier event-assignment/N-A/Simplifi work. **Artists** (`loadArtists`): real talent roster from actors(artist/dj)+event_participants — gigs/fees/last-gig/contact, sortable, click row→booking history; add/edit stays in the Actors tab. Open: most expense vendors are free-text Simplifi payees not yet actors (vendor-link dropdown only lists existing actors); artist fees all $0 (donated sets, no fees logged).

Per-event status:
- **Dance Infusion #1** (`b65d425a`, 2025-09-08): RA tickets itemized — 42 tickets / $1,142.50 in 3 tiers (ticketing, notes='di1-recon-load'); retired the $1,140 income placeholder; +$2.50 RA-fee expense. There was ALREADY an $1,800 expense + $1,800 Keith donation, so **net = $1,140 = donated-to-MS, ties exactly**; total_raised $2,942.50 (gross; $2.50 = RA fees vs the locked $2,940 net headline). 28 buyers in guest_event_attendance (source='di1_ra'). DJs: Berky, KRNeY, **SPF 50** (new actor). Attendance left null (locked rule: RA tickets ≠ attendance). Source of truth: `events/dance-infusion/di-01-2025-09/data/dance_infusion_di1.json`.
- **Maxwell House 4/20** (`e1f8d14d`): series→'Come With Production', type=production; DJs Berky + KRNeY added. ATTENDANCE still needed from Keith.
- **DI Artist Showcase — Kristen London & 32LVS** (`1ebbec24`, 4/18): series→'Content Creation' (recorded for Good Company Online Festival). DJs (Berky, Kristen London, 32LVS) + attendance 25 already present.
- **Knicks G5 Watch Party** (`529cc7a9`, 6/13): confirmed complete as-is (DJs KRNeY/Henry/Berky, Crossroads host, attendance 60).
- **Crossroads Café Artist Showcase** (`c32a537f`, 1/17): 3 free RA RSVPs loaded to guest_event_attendance (source='as01_ra'). STILL NEEDED from Keith: DJ lineup, attendance, correct series.

RA export CSVs live under each event's folder (e.g. `events/dance-infusion/di-01-2025-09/data/*-list.csv`, `events/artist-showcase/as-01-2026-01/*.csv`). See [[project_event_hub_and_di2_money]] for the DI#2 reconciliation pattern this follows.
