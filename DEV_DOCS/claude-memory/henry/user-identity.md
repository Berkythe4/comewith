---
name: user-identity
description: "The user is Henry Zaradich (hjzaradich@gmail.com), working in the Come With repo owned by Keith Berkman."
metadata: 
  node_type: memory
  type: user
  originSessionId: 43d45cf6-b868-4f32-9a6c-033e1bc425b5
  modified: 2026-08-15T19:46:25.171Z
---

The user is **Henry Zaradich**, email **hjzaradich@gmail.com**. Git commit identity is
set repo-locally in `C:\comewith` (not globally) as of 2026-08-15.

The repo (`github.com/Berkythe4/comewith`) belongs to **Keith Berkman (Berky)**, who is
the owner referenced throughout `CLAUDE.md` / `LEARNINGS.md` / `ROADMAP.md` as the
decision-maker ("push only when Keith says so"). Henry is therefore NOT the repo owner —
do not assume push rights to `master`, and remember that `master` auto-deploys to
comewith.org via Netlify.

The project docs name a staff member "Henry" (henry@comewith.org, `sub_admin` /
`operations` staff_role, in the `partners` group, DJ'd Elements Radio Ep3). Likely the
same person, but **unconfirmed** — don't act on that assumption without asking.

See [[comewith-local-setup]] for what's installed and what's still missing.
