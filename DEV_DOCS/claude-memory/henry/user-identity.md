---
name: user-identity
description: "The user is Henry Zaradich (hjzaradich@gmail.com), working in the Come With repo owned by Keith Berkman."
metadata: 
  node_type: memory
  type: user
  originSessionId: 43d45cf6-b868-4f32-9a6c-033e1bc425b5
  modified: 2026-08-22T00:36:23.617Z
---

The user is **Henry Zaradich**, email **hjzaradich@gmail.com**. Git commit identity is
set repo-locally in `C:\comewith` (not globally) as of 2026-08-15.

The repo (`github.com/Berkythe4/comewith`) belongs to **Keith Berkman (Berky)**, who is
the owner referenced throughout `CLAUDE.md` / `LEARNINGS.md` / `ROADMAP.md` as the
decision-maker, and the single `profiles.is_owner` row. Henry is NOT the repo owner, but
**is** one of the three `master_admin`s (with Keith and Martin, promoted 2026-08-15) and
holds **WRITE** on the GitHub repo.

**Henry does direct merges to `master`, and that is a production deploy** — he asked for
push → PR → merge routine → merge in one instruction on 2026-08-21, and it was carried
out. So the old "push only when Keith says so" framing is too strong for Henry
specifically. Still **confirm before pushing or merging** rather than inferring it from
a feature request: `master` auto-deploys to comewith.org via Netlify, so a merge is a
release. Ask once, then act on the answer.

The project docs' staff "Henry" (henry@comewith.org, in the `partners` group, DJ'd
Elements Radio Ep3) is the same person.

See [[comewith-local-setup]] for what's installed and what's still missing.
