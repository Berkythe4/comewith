---
name: personal-xlsx-simplifi-import
description: "Excel-only Simplifi importer (scripts/import_personal_simplifi.py) — separate from Jennifer's DB path"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1bb87365-2460-4e57-ab2a-d85b33c7e8e5
---

There are now TWO Simplifi import paths. Jennifer's (`src/finance_ingestion.py` → planner.db `finance_transactions`) is the original. The second, built 2026-06-18, is **Excel-only**: `scripts/import_personal_simplifi.py` writes solely to `data/Personal.xlsx` and never opens the planner DB.

**Why:** Keith wanted personal-budget actuals in his Excel template, firewalled from Jennifer's finance surface.

**How to apply:** Reuses Jennifer's pure parse helpers (column aliasing, `_normalize_date`/`_normalize_amount`, move-to-`processed/`) copied verbatim — no DB import. Category→envelope rules live in `rules/personal_envelope_map.yml` (17 personal envelopes, NOT Jennifer's). Entity routing: `ComeWith_Budget_Actuals_Model.xlsx` is validated ground truth — (date+amount) match → envelope "Come With"; watchlist merchant not in CW ledger → Status REVIEW (`rules/come_with_watchlist.txt`). Skips `Exclusion=yes` rows. Dedup on (date, amount, description), idempotent vs existing ledger rows. Ledger window was extended from rows 4:2003 to 4:6362 (SUMIFS, dropdowns, Month formula all stretched). Backs up to `Personal.bak.xlsx`.

Refined 2026-06-18 (`scripts/refine_personal_budget.py`, backup `Personal.bak2.xlsx`): added an 18th envelope **Fitness** (personal Variable; Lists A19, dropdown now `A2:A19`, Budget row 22; Fitness/Gym/Workout categories added to rules). Re-tagged in-place 99 Fitness + 18 Entertainment (Cash & ATM + ATM fees) REVIEW rows → REVIEW dropped 391→274. Rebuilt the Budget grid to span the FULL history **2023-01 … 2026-12** (48 month cols D:AY, then Total Actual/Avg/Var/Mos Over at AZ:BC); Budget body now rows 1–29 (Net Cash Flow r29, Total Personal=SUM C7:C22, Business=SUM C24:C25). Targets recomputed: Fitness 111.42, Entertainment 432.52. GOTCHA: openpyxl `ws.cell(r,c,None)` does NOT clear a cell (skips when value is None) — assign `.value=None` instead.

Reworked 2026-06-18 for usability (`scripts/rework_personal_budget.py`, backup `Personal.bak3.xlsx`). Scope narrowed to **move-in = Apr-2025 onward**: older ledger rows KEPT but excluded from the Budget grid + target medians. Budget grid trimmed to 2025-04…2026-06 (15 months) with two collapsible **column outline groups** by year (cols D:L=2025, N:S=2026; M/T year-total summary cols; summaryRight; freeze D4); targets recomputed as Apr-2025+ active-month medians (Income jumped to ~$10.2k). Backfilled Note col G with each row's original Simplifi category from the processed CSV (blank notes only, 5968 rows) so the Dining "Bars" breakdown can key on sub-category. New **Assumptions month-picker dashboard** at $F$2 (validation list from new Lists col E) — Envelope/Budget(INDEX-MATCH)/Actual(picker SUMIFS)/Variance/%used by section + Net Cash Flow. Collapsible **top-vendor child rows** (row outline, summaryBelow=False) under Dining (Uber Eats, Bars via G *Bar*, House Party, Dar 525, Onyx), Shopping (Amazon=*AMZN*+*AMAZON*, Sweetwater, B&H, Apple, Guitar Center), Transportation (Uber, Parking, Auto Repair, Mavis). Token gotchas: `*AMZN*` matches nothing (all `*AMAZON*`); "Wild Wines" is a Bar (don't double-count).

Budget v2 2026-06-18 (`scripts/rebuild_budget_v2.py`, backup `Personal.bak4.xlsx`). (A) **Gear→Come With firewall**: 7 Shopping rows from Sweetwater/B&H Photo/Guitar Center moved to Come With ($3,564.61; 6 matched a validated CW record via date+amount cross-ref against `ComeWith_Work_Expenses_Master.xlsx` [temp read-only, Equipment Capital + Regular Expenses] + CW ledger, 1 Sweetwater $1,052.79 flagged Note "gear→CW (verify)"). **Micro Center $1,684 deliberately LEFT in Shopping** (general electronics, not in any CW record — flagged, don't guess). New Apr-2025+ medians: Shopping 628.51, Come With ~441. (B) **Budget tab fully rebuilt months-down** (replaces wide grid): 21 month blocks 2025-04…2026-12, 4-level row outline (summaryBelow=False): L0 ▼month ▸ L1 Income/FIXED/VARIABLE/BUSINESS/totals ▸ L2 envelope ▸ L3 vendor. Cols A Line·B Budget(editable yellow)·C Actual·D Var·E %used + hidden F mKey/G Envelope/H token. Per-month budgets pre-filled = Apr-2025+ median (past) / median (essentials) or median×0.9 (discretionary: Dining,Entertainment,Shopping,Gifts,Travel,Personal Care,Fitness) for forward 2026-07…12. Transportation kept under FIXED (its Type) with vendor children, NOT duplicated under VARIABLE. Entertainment "Cash" matched by DESCRIPTION (`*WITHDR*`+`*DEBIT*` ≈$9.5k) not Note — the cash sits under the Simplifi "Entertainment" category, not a Cash/ATM note. (C) Dashboard Budget col rewired to `SUMIFS(Budget!B, Budget!G=envelope, Budget!F=picker month)` so picker↔Budget tab stay in sync.

Budget v3 2026-06-18 (`scripts/rebuild_budget_v3.py`, backup `Personal.bak5.xlsx`, Budget tab ONLY — reused v2 budget values, no median recompute). Rebuilt as **5-level row outline** (summaryBelow=False, freeze A3): L0 year (2025/2026) ▸ L1 month ▸ L2 income/section(FIXED/VARIABLE/BUSINESS)/net ▸ L3 envelope ▸ L4 vendor → outline buttons [1]–[5]. Counts: 2 years, 21 months, 105 L2, 357 env, 357 vendor (rows 3–844). **Subtotals live ON every band** via helper-col SUMIFS over the sheet's own cols (B/C/F mKey/G Envelope/H token/I Section/J rowtype, F:J hidden): year=`SUMIFS(B,F,"2025-*",J,"env")`, month=`(…F=$F{r},J,"env")`, section adds `I=$I{r}`, net=`income−env`. So totals stay visible when collapsed. Whole-dollar everywhere (`$#,##0;[Red]($#,##0);"-"`, pct `0%;;"-"`). Firewall preserved: Come With only in BUSINESS section (audited); month/year/Net totals DO include Come With by spec design (J="env"), FIXED/VARIABLE sections do not. Improvements: Variance CF red(over)/green(under) scoped to FIXED/VARIABLE env+section only; tiered band shading; per-level indent; gridlines off; "forecast" tag on 2026-07…12. Full audit passed (reconcile, band integrity, tagging, REVIEW-excluded, firewall, signs, 0 formula errors, dashboard parity, targets).

Budget v4 2026-06-18 (`scripts/rebuild_budget_v4.py`, backup `Personal.bak6.xlsx`) — cleanup + unemployment plan. Context: job ended ~Apr 2026, UI $905/wk≈$3,922/mo confirmed in ledger from 2026-05-21 (NJ), $50k severance modeled as opening reserve (NOT a distinct ledger row — flagged). (1a) Month/year top-line now PERSONAL-ONLY (`SUMIFS FIXED + SUMIFS VARIABLE`, excludes BUSINESS); added Total Personal/Total Business/Net rows per month. (1b) Micro Center $1,683.88 (row 1354, dated 2024-01 so pre-window → medians unchanged: Shopping 628.51, Come With 441) re-tagged Come With. (1c) ALL vendor L4 rows removed → 4-level outline. (PART4) Adjusted budgets written May–Dec 2026 only: Income=UI(3922 May–Oct,0 Nov–Dec); essentials held; Transportation×0.65 (no commute); Groceries 0.97→0.85; discretionary ramp 0.85→0.42 (morale floor, not 0). New sheet **"Adjusted Plan"**: runway model (opening $50k, reserve+=UI−Personal−Business; Business=CW $441/mo modeled unreimbursed), live SUMIFS from Budget tab + editable inputs B5–B8, full assumptions/cut-schedule/BLS-benchmark/research-sources/disclaimer docs. Runway: adjusted reserve $0 ≈ Jan 2027 vs no-change ≈ Dec 2026 (ramp preserves ~$13.8k); May final pay ~$14k = extra unmodeled cushion. GOTCHA: LibreOffice/Excel must be CLOSED to save (PermissionError; check `.~lock.Personal.xlsx#` + soffice.bin). v4.1 restyle 2026-06-18 (backup `Personal.bak7.xlsx`): calmer palette (navy year / blue month / light-blue section / white envelope / gray totals / soft-gold Net), clearer month grouping (top borders per month), redesigned full-width "Adjusted Plan" summary (banded runway table + CF red on negative reserve + stacked docs). GOTCHA: on rebuild, openpyxl `put`/cell writes that DON'T set `.fill` leave the PREVIOUS layout's fill in place (rows shift between versions → stray blue env rows). Fix: in the wipe loop fully reset every cell (value/fill/font/border/alignment/number_format), not just `.value=None`.

Runway v2 2026-06-18 (`scripts/update_adjusted_plan_v2.py`, backup `Personal.bak7.xlsx` = now the clean restyled state; Adjusted Plan sheet ONLY, Budget/ledger/dashboard untouched). Real numbers: opening reserve **$100,000** (Wealthfront Cash, the user's actual liquid cash — NOT the $50k severance), interest **3.30% APY → APY/12 ≈ 0.275%/mo ≈ $275/mo on $100k** applied to running balance (annual rate paid monthly; 3.80% if +0.50% boost active). Expenses pulled LIVE from Budget tab Total Personal + Total Business (totbus=0, CW assumed reimbursed) + editable Healthcare-actual gap (default $107; flag COBRA $500–2000). Runway table Month·Reserve start·UI·Interest·Expenses·Net·Reserve end, CF red on negative reserve. **Zero dates: this plan ($100k+cuts) ≈ Sep 2027 · $50k+cuts ≈ Feb 2027 · no-cuts $100k ≈ Jun 2027.** HEADLINE = monthly earned income to break even (reserve stops falling): during UI ≈ $3,968/mo, after Oct cliff ≈ $6,896/mo, with −0/−10/−20% expense sensitivity (all live formulas).

Corrections v5 2026-06-18 (`scripts/corrections_v5.py`, backup `Personal.bak8.xlsx`; targeted Budget cell edits — NOT a rebuild — + Adjusted Plan rebuild + Assumptions restyle). (1) **Insurance is semi-annual**: budget set to $1,100 in June & December only ($0 all other months, past+forward) — fixes the inflated ~$1,112/mo. (2) **Healthcare = COBRA**: $300/mo May–Oct 2026 (subsidized), $900/mo Nov–Dec (subsidy ends); pre-May untouched. (3) Reserve $100k, interest 0.275%/mo, runway now has a **Milestone column** flagging the $50k-consumed month and $0 month. (4) Corrected runway: **$50k consumed ≈ Mar 2027 · reserve $0 ≈ Sep 2027 · no-cuts $0 ≈ Jun 2027**. Required earned income to break even: **during UI ≈ $3,232/mo · after Oct (full $900 COBRA) ≈ $7,127/mo · after Oct on ACA Marketplace ($450) ≈ $6,677/mo**. October = double cliff (UI→$0 AND COBRA $300→$900) — flagged on sheet. (5) **Assumptions dashboard restyled** to the clean palette (navy header / blue section bands / white envelopes / gold Net / faint-yellow picker; Variance as red/green TEXT via CF, no fills; gridlines off) — picker + all formulas intact. no-cuts baseline fix: exclude insurance/healthcare from the 2025-04 median and overlay corrected per-month values so the comparison is honest.

Consolidated v6 2026-06-18 (`scripts/consolidated_v6.py`, backup `Personal.bak9.xlsx`; Budget+Adjusted Plan+Assumptions). **SIGN CONVENTION flip** (big readability change): expenses now NEGATIVE/red, income positive; Actual = signed `=SUMIFS` (removed the `=-SUMIFS` negation everywhere); expense budgets negative (Housing −3031 etc.); Net = signed sum (Budget+dashboard Net formulas flipped `-`→`+`); Variance=C−B reads intuitively → CF green ≥0 / red <0 (replaced old red-when-over). Number format `$#,##0;[Red]-$#,##0;"-"` (red neg) on body, `$#,##0;-$#,##0;"-"` (white) on navy/blue bands; Variance CF excludes band rows for contrast. Insurance −1100 Jun/Dec only; Healthcare COBRA −300 May-Oct / −900 Nov-Dec. **Come With = separate burn track**: gave Come With a −441/mo budget so Total Business=−441; runway has 3 tracks — Personal ($50k reserve, editable B5→100000), Come With ($5k reserve, ~$441/mo, no income), Combined ($55k). Personal track has an editable **Earned-income** yellow column (default 0) that drives the runway live (Net=UI+Earned+Interest+PersonalExp). Dates (earned $0): personal $0 ≈ Mar 2027 (=$50k consumed since reserve is $50k), CW $0 ≈ Apr 2027, combined $0 ≈ Mar 2027; at break-even earned ~$3,370/mo personal lasts to ~Dec 2027. Required earned income: during UI ≈$3,370 · after Oct(COBRA $900) ≈$7,264 · Marketplace ≈$6,814; −10/−20% sensitivity. Dashboard restyled to clean palette + signed. Related: [[cwf-deepverify-revisions]].
