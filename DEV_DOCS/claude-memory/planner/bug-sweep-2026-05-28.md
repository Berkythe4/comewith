---
name: bug-sweep-2026-05-28
description: "Cleared all 5 open jennifer_bugs (#548/#549/#563/#550/#551) on 2026-05-28; sprint tag \"bug-sweep 2026-05-28\""
metadata: 
  node_type: memory
  type: project
  originSessionId: 23962f70-e541-4462-96aa-41148968baf0
---

On 2026-05-28 the entire open jennifer_bugs queue (5 bugs) was fixed and marked `status=fixed`, `fixed_in_sprint="bug-sweep 2026-05-28"`:

- **#563** — block-pane start/end times were UTC-skewed; convert UTC↔local in `renderBlockPanel` (`static/calendar.js`).
- **#549** — per-task ✕ "remove from block" button in the block pane (detach → unscheduled).
- **#548** — `_safe_write` in `src/web_server.py` swallows `ConnectionAbortedError`/`BrokenPipeError`/`ConnectionResetError`.
- **#550** — drag an open task onto a working block to attach it; calendar `drop` handler hit-tests blocks via `data-block-id` and POSTs to `/api/blocks/<id>/tasks`.
- **#551** — calendar now defaults to Week and adds "Next 4 days" (`timeGridFourDay`) + Month views; left-pane Open⇄Scheduled toggle groups tasks under upcoming blocks.

Tests: `tests/test_bugs_548_549_563.py`, `tests/test_bugs_550_551.py`; `tests/test_calendar.py` updated for the new view defaults.

**Why:** records that the bug queue was emptied and where each fix lives, so a re-report of any of these is a regression, not a new bug.

**How to apply:** the jennifer_bugs queue is the source of truth — `/bugs` (or query the table) shows open items. See [[preexisting-frontend-test-failures]] for the unrelated full-suite failures present at the same time.
