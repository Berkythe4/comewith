# Planner-project Claude memory (desktop)

These are the memories for the **AI Planner** checkout
(`C:\Users\Admin\Documents\Master\planner`), not this repo. Claude Code keys memory
by checkout path, and a lot of Come With's finance work is driven from the planner
side — the `uf_*` unified-finance model, the Simplifi / PayPal / Bluevine importers,
and the push into this site's `ingest-finance` function.

Snapshotted here so the other machines can see what the planner session knows.

**Do not copy these into the parent folder.** That root holds the desktop's ~66
Come-With memories and its own `MEMORY.md` index; overwriting it with this 12-entry
index would lose the map to files that are still sitting there. Same rule as
`henry/`. See `MERGE_ROUTINE.md` → "Every close, regardless of size".
