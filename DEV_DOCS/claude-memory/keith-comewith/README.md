# Claude memory snapshot — Keith's `C:\Users\keith\comewith` machine

A **third** machine, distinct from the two already snapshotted here:

| folder | path slug | notes |
|---|---|---|
| `DEV_DOCS/claude-memory/` (root) | `C--Users-Admin-Documents-Comewith` | the desktop; the large original |
| `DEV_DOCS/claude-memory/henry/` | Henry's machine | |
| `DEV_DOCS/claude-memory/keith-comewith/` | `C--Users-keith-comewith` | **this one** |

Claude Code's memory is per-machine and per-path and does not sync, so this
folder is a committed copy of what that machine knows. It started **empty** on
2026-08-21 and these four files are its whole contents — read the root snapshot
as well, which is far larger.

Same rules as the root `README.md`: the live memory on the machine is the
original and this is a copy; memories record what was true when written, so
verify any file, column or flag they name before acting on it; and no
credentials, because this folder is in git.

Snapshot date: 2026-08-21 (4 files).
