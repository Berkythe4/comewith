---
name: project_customer_frontend
description: Customer front-end redesign — 3 local prototypes + research dossier in prototypes/customer-frontend/ (NOT deployed); current live homepage is index.html
metadata:
  type: project
  originSessionId: 23f44bb5-a672-44a4-9c2e-b8eac9975d80
---

Customer-facing site redesign explored 2026-06-26 (commit ac9f3ff), built **locally only** in `prototypes/customer-frontend/` — does NOT touch the live homepage `index.html` (which is the current "stripped to basic" version: dark plum/cream, DM Sans/Mono, events modal + contact modal, @comewithnyc / berky@comewith.org).

**Three research-informed single-file prototypes** (share `_shared.js` = live data layer):
- **v1-pulse.html** — after-dark/nightlife (party-goers). Archivo + DM Sans, dark plum/lime/pink, experience-first hero + next-show ticket card + marquee.
- **v2-marquee.html** — editorial/premium studio (bookers/sponsors). Fraunces serif, cream, 3 capability lanes + "Request a proposal" + work-as-case-studies.
- **v3-infusion.html** — community/mission-first (donors). Poppins, warm cream/rose, **live impact counters + progress-to-$10k-goal bar + tiered $X=outcome giving + one human story + Safer-Space line** (the charity toolkit).

**`_shared.js`** wires LIVE: upcoming events from `v_public_events` (anon REST) + inquiry submit to `inquiries` (anon insert, source='website'). Publishable key `sb_publishable_IkigzWOTU3ZSMK9DxqwwJw_AaQkShCi` works for REST (verified). Newsletter routes through inquiries as a tagged signup (real subscribe Edge Function exists if wanted). Imagery is placeholder gradients (needs real event photos); ticketing links out to RA per research.

**Docs in the folder:** `RESEARCH.md` (sourced: NN/g/Baymard/web.dev/W3C design principles + event/ticketing UX teardowns + **13 comparable companies** — 23 Layers, Eventique, Empire, Nowadays, Public Records, Lot Radio, Elsewhere, Teksupport/TCE, Cityfox, Dancers Against Cancer, Sweat with Pride, Daybreaker, Mirrored Media; every claim has a verified URL). `COMPARISON.md` (scorecard + **recommended hybrid: V1 Pulse base + V3's Dance Infusion block + V2's proposal lane**). `CONTENT.md` (real brand facts). `README.md` (how to open). Research done via 3 parallel general-purpose WebSearch/WebFetch agents.

**Audit:** structural + reference-integrity + JS syntax passed on all 3; caught + fixed a real malformed `\'`-escape bug that had broken the form/newsletter inline scripts (lesson: avoid escaped apostrophes in JS string literals written via the Write tool — use double quotes).

**HYBRID built (2026-06-26, commit 3c8ee8d):** `v4-hybrid.html` = the approved merge (Pulse dark base + V3 Dance Infusion impact block w/ live progress bar to $10k goal + tiers + Safer-Space + V2 production "Request a proposal" lane). **Admin-curated Recent Rooms** via migration **061** (events.is_featured + youtube_url + recap_blurb; v_public_recap anon view; hero photo reuses events.hero_image_path → public event-photos bucket). Dashboard edit-event modal gained a "Recent Rooms (site recap)" section: Feature toggle + blurb + YouTube URL + **hero-photo upload** (event-photos has Admins-upload/Public-read storage policies). Recent Rooms cards show the photo + a click-to-embed **YouTube lightbox** (ytId parser handles youtu.be/watch/embed/bare). Newsletter wired to the real **subscribe Edge Function = DOUBLE OPT-IN** (pending → confirm email → subscribed; user wanted "subscribed" — flag: offer instant-subscribe). _shared.js gained fetchRecap/subscribe/ytId. Still LOCAL (not yet swapped to index.html).

**Photo workflow (answered):** Google Drive = fine as the master source library, but the site can't serve from Drive (not a CDN; links break/rate-limit). Choose photos by uploading one hero per featured event in the dashboard → served from Supabase event-photos bucket. Multi-photo gallery is a future add.

**WENT LIVE 2026-06-26 (commit e21af1b, pushed → Netlify):** old 916KB homepage replaced by the content-driven V4 hybrid as `index.html` (backed up to `backups/index.html.pre-redesign-2026-06-26` + tag `backup/2026-06-26-pre-frontend`). New `watch.html` = dedicated content/watch page (recap videos from v_public_recap as YouTube thumbnails + lightbox). Tiny CMS via **migration 062** `site_content(key,value)` (public read; write gated to `site-editor` module / master) — 54 keys seeded; every site text element tagged `data-cw="key"` with inline default fallback. Added daytime/accessible/all-ages **community** section + DI **progress bar** reading `di.goal_total`=10000 / `di.goal_raised`=4140 / `di.goal_label` ("$10K across DI#1→DI#3, next this October"). **Logo system:** `data-logo` slots + CSS mask+tint recolors the brown logo to blend (`brand.logo` URL + `brand.logo_tint` cream/lime/white/plum/none) — ONE backend control updates everywhere. Dashboard **Site Editor** (panel-site-editor, module 'site-editor' in nav group Insights, default_roles marketing/full; loader `loadSiteEditor`/`renderSiteEditor`/`saveSiteContent`): edits all 54 keys grouped by section + logo upload (event-photos `site/` prefix, getPublicUrl) + tint select, one save goes live.

**E2E tested on live prod (all pass):** home + watch 200 w/ content-driven text; site_content 54 keys anon; v_public_recap 3 rooms + v_public_events 1 upcoming anon; anon inquiry POST 201 (full_name/services_selected/source shape — note col is `full_name` NOT `name`); subscribe = DOUBLE OPT-IN (status pending + confirm_sent, kept per Keith); financial views still 401. **ONE manual step left for Keith:** upload the actual logo file via Site Editor (brand.logo is currently empty → site shows the "COME·WITH" wordmark); tint preset to cream. Recap videos: blurbs seeded but youtube_url null → add per-event in dashboard edit-event modal. See [[project_public_events]], [[project_email_campaigns]].
