---
name: radio-release-pipeline
description: "Come With Radio episode pipeline (099, 2026-07-20) — numbered stations, mix→SoundCloud, public radio.html w/ listener accounts, song memory"
metadata: 
  node_type: memory
  type: project
  originSessionId: da096138-2fcd-43c9-bd0a-616b0f501ac3
  modified: 2026-07-23T16:22:55.249Z
---

Come With Radio release pipeline SHIPPED 2026-07-20 (commit a3b00d2, migration 099 applied to prod, get-station + sc-connect redeployed, Netlify live).

- **Stations = numbered episodes** (`sc_playlists.station_no`, lifecycle `building→testing→live/archived`; partial unique index = only ONE building row). Dashboard Artist Radio has a station switcher + 📜 Song History.
- **Flow:** ① export tracklist → private SC playlist (test) → ② reorder on SC, sync back (cut songs logged as *passed* in `sc_song_log`) → ③ 🚀 Go live modal: attach mix (upload ≤50MB → `radio-mixes` public bucket → API `POST /tracks` private, OR paste SC track link — resolve path verified live on prod), slug + two descriptions; finalize publishes page, pushes short desc + `sharing=public` to the mix track, and creates next EP pre-seeded with carried (passed-not-played-not-carried) songs. `carried_to` set once = no re-carry loops.
- **`sc_song_log`** = permanent song memory (played/passed/carried + EP numbers/dates); drives ✓ played / ✋ passed marks + "hide played" in the song browser. Manual ✕ remove also logs passed.
- **Public radio.html**: hub (`get-station?list=1`), episode pages `?s=slug` (published only), `?t=token` unlisted preview. CSV export (BOM). **Listener accounts** = Supabase magic link (redirect allow-list already covers comewith.org/**), profiles default role customer; `listener_playlists/_tracks/_station_history` owner-RLS, anon-revoked. Non-blocking signup nudges (first-visit pill + export modal w/ "Just download").
- **Phase 1.1:** YouTube auto-post at finalize (needs OAuth upload scope + video render; manual URL field `mix_youtube_url` for now). **Phase 2:** Spotify/Apple Music — likely NOT viable for mixes of other artists' tracks (licensing); reassess.
- Test rows (EP 990/991) created + deleted during e2e; prod left with only EP 1 "Weekly station" (building, 21 tracks). Anon security re-verified (financial views 401, listener tables 42501).
- **Scheduled drops (100, same day):** `sc_playlists.drop_date` = radio's own release tracker (DECISION: radio stays OUT of the events/series system; Keith's "Come With Radio Episode 1" placeholder event 76924558 in Content Creation is superseded — he can delete it). EP 1 drops **2026-07-23**. get-station list returns `next_drop`; homepage pill under Next Up teases it then flips to "New — listen"; radio hub shows a drop banner; ticker (`strip.items`) includes "Come With Radio"; finalize also drops a stage=posted card on the social calendar.
- **Beatport:** no self-serve API access + no purchase API (verified 2026-07-20); pattern = Keith's own login token w/ Beatport docs-frontend public client_id. `/beatport-cart` project skill (`.claude/skills/beatport-cart/`) = on-demand cart build: match station tracklist → add to Keith's cart (internal /v4/my/cart, introspect-first) → fallback buy links. Token lives in gitignored `.beatport_token.json` (NEVER site_content — anon-readable); first run needs Keith's browser token paste.
- **SoundCloud share links break the embed player:** `on.soundcloud.com/xxxx` (and any share/short link) is a 302 redirect the `w.soundcloud.com/player` widget does NOT follow → set won't play. Always resolve to the full `soundcloud.com/user/track` URL before storing in `events.recap_videos`/`youtube_url` or station tracks. Fixed DI#1 (event b65d425a) 2026-07-21 this way. `mediaKind()` matches "soundcloud.com" so the card still renders — the failure is silent (player just won't load).
- **ROOT-CAUSE FIX (2026-07-21): `resolve-media` edge fn** (admin) normalizes+validates any recap/media URL: follows short links, strips utm_*/si, verifies PUBLIC embeddability via provider oembed (SC/YouTube). Event editor (submitEditEvent) auto-normalizes good links on save + blocks silent failures (private/secret/wrong link → flagged w/ reason, "save anyway" override), plus a "✓ Check & clean links" button. oembed 404 = not publicly embeddable (private/secret set, wrong permalink). DI#2's `soundcloud.com/berkythe4/sets/dance-infusion-2` is PRIVATE — no public DI#2 recording exists on berkythe4; needs Berky to make it public or supply a public URL. Sweep 2026-07-21: only DI#2 bad of 12 recap links.
- **Benefit recap tiles hero-photo bug (fixed 2026-07-21):** `.ph.benefit`/`.thumb.benefit`/`.thumb.audio` used the `background:` SHORTHAND which resets background-size/position/repeat → benefit events w/ hero photo rendered zoomed top-left. Fixed to `background-image:` only (index.html + watch.html). Rule: never use `background:` shorthand on a state/variant class layered over a base that set size/position.
- **DI#1 homepage note:** it's the 7th-newest `is_featured` recap and index.html "Recent rooms" shows only newest 6, so DI#1 lives on watch.html (full archive), not the front page. `v_public_recap` filters on `is_featured` (NOT is_public).
- **Radio dashboard UX (2026-07-21):** sticky filter/search toolbar (position:sticky top:0) + collapsible station builder (raState.stationOpen, default collapsed) so the searchable artist list sits under the filters; default sort now Artist A–Z; scroll position preserved on expand-songs/add-track. Same `.se-bar`/sticky pattern; `.main` is the scroll container (overflow-x:auto forces overflow-y:auto).
- **REKORDBOX PIVOT (2026-07-22, commit 5c92688, migrations 102+103 applied to prod):** SoundCloud isn't record-quality, so Keith BUYS the songs and arranges in Rekordbox. ① SC test push + ↺ sync-back KEPT (first pass), but **Rekordbox owns final order**. "🎛 Import Rekordbox order" parses the playlist export (UTF-16 tab TSV, columns located BY HEADER NAME; also .m3u8/CSV/pasted/our own Publish-tracklist output), fuzzy-matches, shows the diff BEFORE writing, applies order + BPM/key (accepts musical or Camelot, fills the other side). Left-out songs → logged passed + carried (same contract). "＋ Add song"/✎ = hand-added tracks (bought/promo): `source` in ('soundcloud','manual','rekordbox'), synthetic `man_<uuid>` sc_track_id keeps dedupe + song memory + carry-over working; `buy_url`/`label` PRIVATE (get-station doesn't select them); linkless tracks render as a text row on the episode page.
- **"🛒 Where to buy" = `track-sources` edge fn (Beatport reversal).** Beatport is now an ALWAYS-ON integration (was skill-only) — real v4 catalog API, gives availability + price + bpm/key/label. Tokens in `public.beatport_oauth` (admin-RLS, anon-revoked) NOT an env secret, because Beatport ROTATES the refresh token every use and a function must be able to write it back. Secrets `BEATPORT_CLIENT_ID`/`BEATPORT_REFRESH_TOKEN` seed it. Bandcamp = NO official API, unofficial autocomplete, best-effort → report "couldn't reach" never "not available". **Store metadata only FILLS IN missing bpm/key — never overwrites Rekordbox.** **Remix guard** in the matcher: if either side names a remix/edit the remixer must match, else you buy the wrong mix.
- **Public page never links the SOURCE playlist** (decision 2026-07-22): get-station no longer selects `sc_playlist_url`; radio.html no longer links OR embeds it. Listeners get the final mix (SC track / YouTube) only; to get the songs they come to the page and export. Per-track ☁ links KEPT.
- **103 also revoked anon on `sc_playlists`/`sc_playlist_tracks`** — they'd carried table-level anon grants since 079. RLS was always blocking rows (anon GET = `200 []`, never data); now `401`. Found by post-apply check; no leak ever occurred.
- **Listener SoundCloud OAuth export: REJECTED 2026-07-22, do not revive without new information.** All risk, no incremental reward: every listener export burns YOUR app's SC quota (competing with your own publishing), one abuse report can suspend the app that publishes your episodes, and **SoundCloud app registration is closed so it's unrecoverable**. The ♡ save already earns the signup AND the data; the export was only delivery convenience. Instead: saved tracks expose their SoundCloud link (per-track ☁ + copy-all-links) so listeners go there themselves.
- **LISTENER FEATURES SHIPPED 2026-07-22 (commits b103ba0 + 16fa4dd, deployed + pushed, verified live):** `listener-shows` edge fn = "🎟 Playing New York soon" in the ♡ drawer (artists you saved with an upcoming RA show + date/venue/price/ticket link) — server-side so `ra_artists` stays admin-only, and matching is **exact-after-normalization on purpose** (a fuzzy hit would send someone to buy a ticket for the wrong act). Plus ♡ Save all (skips saved + linkless), drawer sort Recent/Artist/BPM/Camelot (camelotRank sorts numerically so 10A ≠ between 1A and 2A), per-track ☁ link, copy-all-links.
- **"❔ What you get" guide** — always in the radio nav signed in or out; 5 plain-language numbered steps; reads as the pitch pre-signup and the manual post-signup, re-renders in place when auth flips. Keith's standing want: the value of an account must be unbelievably easy to understand and always reachable.
- **Mailing list from radio = `come_with` segment ONLY** (checkbox at signup + join button in the guide). Dance Infusion is a separate list, never opted into from radio — verified 0 occurrences of `dance_infusion` in live radio.html. Double opt-in; sign-in never blocks on the list call. **Account ≠ mailing list ≠ actor ≠ customer** — four distinct things; the guide now says which is which.
- **config.toml verify_jwt trap (fixed 2026-07-22):** `get-station` + `sc-oauth` were deployed by hand with `--no-verify-jwt` and NOT recorded in config.toml — a plain `supabase functions deploy get-station` defaults verify_jwt=TRUE and would make the public episode page demand a login. All four radio fns now pinned in config.toml (get-station/sc-oauth false; listener-shows/track-sources true). Deploy with `SUPABASE_ACCESS_TOKEN=$SBP_PAT supabase functions deploy <fn> --project-ref yaytdosxfhcqatmhctzk`.
- **Identity model clarified for Keith:** listener = `auth.users` + `profiles.role='customer'`. NOT an actor (actors = artists/venues/vendors/sponsors) and NOT automatically a `subscribers` mailing-list row — account signup and mailing list are SEPARATE today; joining them needs a deliberate consent hook (and must never re-subscribe a global-unsubscribed email).
- **VIDEO PIPELINE (Option 4 "Now Playing") SHIPPED 2026-07-22, end-to-end tested.** Keith records the mix on a standalone controller (NOT laptop Rekordbox), makes an Option-4 video (per-track card: cover + artist + title + BPM/key chips + up-next + lime progress bar), uploads to YouTube (the phone+TV reach answer; Spotify/Mixcloud rejected — Spotify won't take mixes of tracks you don't own, buying ≠ redistribution rights). **NO ticket/ad language in the video** (Keith's directive) — show info lives on the site only. Tools live in **`Radio/render/`** (new top-level `Radio/` folder: Artwork/Video/Documents/render):
  - `make_episode.py --audio X.wav [--history H]` = ONE command: pull cues → times → render `Radio/Video/EP{N}.mp4`.
  - `render_episode.py`: Pillow draws per-track PNG cards (Arial Black + Consolas ≈ Archivo/DM Mono; brand tokens from radio.html), ffmpeg concat-holds each for its duration + overlays a time-gated lime progress bar. **ffmpeg gotcha: `drawbox`'s `t` is THICKNESS not time — use `geq` alpha (`x < BAR_W*t/total`, needs r/g/b AND a) + `overlay`.** Refuses to render on any blank start time.
  - `make_cues.py` / `weekly_prep.py` (READ-ONLY prod pulls): cues CSV + YouTube text/chapters + buy list + pre-ticked release checklist → `Radio/Documents/`.
  - `import_history.py`: Rekordbox/Engine history export → cue times (clock→offset, by order), **preview-first, never writes without --write**.
  - `tap_times.html`: offline tap-along (play mix, tap space per track, export cues).
  - Timestamps for the video: easiest = deck **HISTORY** on the USB (Pioneer/Engine log it standalone → import to software after → export → import_history.py), else tap-along. Do NOT capture during recording. Audio-boundary auto-detect deliberately NOT built (high risk).
  - Revert = delete `Radio/render/` (all additive local files, no prod/app deps). Generated `EP*` files gitignored.
- **Deliberately NOT automated (launch-eve, low-risk stance):** YouTube upload API (Phase 1.1), scheduled RA pull (cron→function auth unsolved), auto mailing blast at go-live, dashboard video button. All flagged for later, not blockers.
- **AUDIO-MATCH TIMESTAMP RECOVERY (2026-07-23, commit for match_mix.py):** rekordbox's built-in recording `.cue` came out EMPTY (no track markers), and the History `.m3u8` has full track lengths not mix positions — neither carries per-track start times. So `Radio/render/match_mix.py` recovers them: ffmpeg→log-mel spectrograms→FFT cross-corr with ±8% tempo search, using the KNOWN ordered tracklist + a **Viterbi/DP global monotonic alignment** (greedy sequential cascades — one bad peak ruins everything after). Outputs tracklist.json + youtube_chapters.txt + match_report.md (flags low-conf/suspicious/missing-source). Missing-source tracks interpolated. Keith reviews + hand-corrects `start_sec` in tracklist.json; render reads ONLY that JSON. Source tracks were at `G:\Radio Station\Week 1\Tracks` (m3u8's E:\ paths stale). EP1 = 60:33, 18 tracks, delivered.
- **VIDEO CHIPS = show date+venue** (not BPM/key, Keith's call). render_episode `--json tracklist.json` drives timing; show info embedded per-track in the JSON so reordering (e.g. Keith swapped Luke Dean before Boys Noize by actual play order) can't misalign venues. Progress bar BAKED into each PNG (stepped) — the per-frame ffmpeg geq bar was ~18s/frame + OOM on an hour render.
- **WEEKLY FOLDERS (2026-07-23):** each episode = `Radio/Week N/` (mix, history, tracklist.json, chapters, cues, docs, finished `CWR_EpN_YouTube.mp4`). `render/` = tools only; `Artwork/` = station art. Legacy Video/Documents removed. Heavy media gitignored. Tools take `--week N`: `make_episode.py --week 1` auto-finds everything + renders there; prefers a verified tracklist.json over history/cues. weekly_prep/make_cues also `--week`.
- **PROCESS MAP ON THE SITE:** `radio-workflow.html` (comewith.org/radio-workflow.html, noindex) — end-to-end weekly run, linked from Artist Radio panel (🗺️ Workflow). Step 2.4 = export track times at RECORD time (* caveat re empty .cue); 3.1/3.2 clarified (save to Week N/, one command); folder layout shown.
- **SCHEDULED GO-LIVE (106, applied 2026-07-23):** DB-ONLY publish — Keith schedules YouTube+SoundCloud release on those platforms; the site just flips the page live at the same time via pg_cron. `sc_playlists.scheduled_go_live` + `radio_open_next_station()` (shared, idempotent) + `radio_schedule_go_live()` (admin RPC, is_admin-gated) + `radio_publish_station()` (SQL half of finalize: publish page, log played, open next carry-over station, social card — NO SoundCloud API push) + `radio_publish_due()`; pg_cron `radio-publish-due` every 5 min. **Functions default to EXECUTE for PUBLIC — must revoke from PUBLIC not just anon** (SECURITY DEFINER hole); verified anon=none, authenticated=schedule-RPC-only. Scheduling **opens next week's station immediately** (flips current building→testing to keep the one-building invariant) so Keith works ahead + flips via the switcher. Go live modal has "⏰ Or schedule it"; ⏰ badge on header. Verified with BEGIN/ROLLBACK test (station→live, played logged, next opened+carry-over, prod untouched).
- **✎ Details now has a YouTube link field** that saves WITHOUT publishing (mix_youtube_url).
- Artifacts (private, claude.ai/code): video style mockups + process map (now also the permanent radio-workflow.html on the site).
- Related: [[project_ra_market_radio]]
