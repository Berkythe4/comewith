# Session review — 2026-06-02

**The arc:** build a public Dance Infusion #2 impact report → the **money framing** turned out to
be the whole story, and it went through several corrections before locking:
1. **"raised" vs "donated"** — the hero must say **"$3,000 to the National MS Society"**, never
   "raised" (the ~$9K gross can't sit next to the $3K on the report — bad optics, Liz's note).
2. **expense ratio (~70% cost / $0.70 per $1)** — read as a *miss*, too negative for a public page.
3. **landed on "% to the mission" (31%)** — positive, donor-native (donated ÷ raised), paired with a
   **60% forward commitment from DI #3 on** (NOT a bar DI #2 was measured against).
DI #1 → DI #2 was reframed as **absolute growth** (donated **2.6×** $1,140→$3,000; raised **3.3×**
$2,940→$9,557), explicitly *not* an efficiency improvement (% to mission fell 39%→31% as we scaled —
truthful, stays on the audit, never spun as a rise). Then built a **reusable admin-gated `/staging/`**
area reusing the dashboard's Supabase sign-in, so review-before-publish pages sit behind one front door.

## What happened (files only — no migrations, no DB)
- Built DI #2 **impact report** + **public audit** (live from `dance_infusion.json` + `dance_infusion_di1.json`); PDF export; sponsors by tier.
- Revised to the % to mission framing (brief was updated mid-stream); removed all expense-ratio language from the public pages.
- Added the understated **founder-contribution note**; $9,557 = $6,557 costs + $3,000 donated (incl. $130 Crossroads + ~$162 founder).
- Built `/staging/` — `guard.js` shared gate, manifest-driven hub, README; **2-line drop-in include**; reuses the `is_admin` pattern.
- Confirmed DI #1 data; **renamed** folder `di-01-2024-09 → di-01-2025-09` and fixed the report's fetch path + `series_summary.md`.
- Removed a duplicate "Partners" block from the report.

## Key decisions (rationale in LEARNINGS.md)
- **§8** % to mission (public) vs expense ratio (internal); 60% = forward commitment from DI #3; DI #1→#2 = growth, not efficiency.
- **§9** $9,557 total-raised reconciliation (gross from others + Crossroads $130 + ~$162 founder contribution).
- **§10** staging = reusable client-side admin gate; sensitive data stays in Supabase RLS, not static files.

## Verification
- **Anonymous gate live-verified** (headless Chrome: report/audit/hub all redirect to `/dashboard.html`, content not painted). Signed-in admin/non-admin branches code-verified; Keith confirmed admin-sees + incognito-redirect live.
- All 5 financial views anon-**401** at close.

## Parked / next
- **CONSENT SWEEP** — the hard gate before the report can go public (sponsors, team, artists, raffle donors; identify Yankees-hats donor).
- Fill placeholders (human-moment quote, hero/inline photos, social reach, what's-next).
- To publish: remove the 2 guard lines from the report + audit.
- Dashboard `di.cost_to_raise` internal target → 40% expense / 60% to mission, to reconcile with the audit.
- **CWF BRD remains the calendar priority** (June 8 soft / 15 hard); Come With is maintenance-only.

## Honest note
The report is built and gated, but it **cannot go public** until consent is gathered and the
placeholders are filled — those are people-dependent, not code. Also: this session's close-out
commits are **local and unpushed** by request.

---

# Session 2 (same day) — Data architecture: deploy 023–028 to prod + populate with DI data

**The arc:** took the held data-architecture build (023–028) **live on prod**, then **populated** the
actor/event model with reconciled DI #1/#2 data. Two threads proved out:
1. **The money model survived heavy correction** — raised vs donated vs % to mission; the **$9,557
   reconciliation** ($6,557 expense + $3,000 donated); and the call that **founder out-of-pocket = a
   donation attributed to Keith-the-actor**, counted in raised (not a special excluded bucket). The
   schema has no native "% to mission" — it reconciles as **1 − cost_to_raise** because we model
   **net_pl = donated** (DI#1 2940−1800=1140; DI#2 9557−6557=3000).
2. **The actor model proved itself on real overlapping-role data** — **Keith = dj + donor + sponsor +
   team** (Berky is Keith); **Crossroads Café = vendor + sponsor**. One identity, many roles, no
   duplication — exactly the unlock the model was for.

## What happened
- Applied **023–028 to prod** (per-migration, anon-401 held); hand-fixed `events.type` placeholder mis-categorization.
- **029**: `sponsorships.sponsor_id` nullable + `actor_roles` `donor` role.
- **DI#1 dup resolved** (reference assumption was inverted: "Dance Infusion #1" was the deleted shell, "Dance Infusion MS" the live data) → restored canonical DI#1, moved data, deleted a junk ticket row, soft-deleted the dup → **39% to mission**.
- **DI#2**: expenses by cost group + $130 Crossroads + $162.44 founder + $6,225 sponsor cash → **31% to mission**.
- 12 new actors, 5 DI#2 DJ participants, 12 sponsorships; verified role-overlap, no dup actors, money math, anon-401.
- Earlier same day: pushed the held build to master (drift closed), tools deployed + gated.

## Key decisions (LEARNINGS §11–14)
guests = lighter layer (promote on recurrence); founder money = donation attributed to Keith; attendance = ticket-sales proxy, basis-tagged; % to mission = 1 − cost_to_raise.

## Parked / next
- 3 actor-inspector verifications: "19th & 7th Productions" (merge into Keith or separate?), DJ↔contractor matches, Yankees-hats donor (unidentified).
- Held: `261797d` + `5cbb51e` + this close-out commit — push pending Keith's go.
- Hard blocker before any customer/external login: the financial-view security fix.

## Honest note
DI financials are now in prod as **real money under reconciled definitions** (founder contributions are real $), but some detail is **consolidated** (DI#2 ticket+bar = one income row; per-line detail stays in `dance_infusion.json`) and donations are attributed by **text donor_name** (no actor FK yet). Fine for headline KPIs; flagged for later.
